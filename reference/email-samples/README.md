# Email Samples (Reference Only)

This folder contains example email templates in HTML and React (TSX) format.

- `html/` – raw HTML samples, for design and structure reference.
- `react/` – React/React Email (TSX) samples, for component/layout reference.

These files are **not** used directly at runtime by the app. They are for reference and inspiration only.
